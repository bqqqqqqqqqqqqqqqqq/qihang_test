<van-cell title="学生总数" value="{{total}}" size="large"  />
<van-index-bar index-list="{{ indexList }}" wx:for="{{ sList }}"  wx:key="index">
  <view>
    <van-index-anchor index="{{ item.grade }}">标题1</van-index-anchor>
    <van-cell title="{{ item.name }}" size="large" value="{{item.grade}}" wx:for="{{ item.AllStu}}" wx:key="index" />
  </view>

</van-index-bar>

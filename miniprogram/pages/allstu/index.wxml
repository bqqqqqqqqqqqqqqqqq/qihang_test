<van-cell title="学生总数" value="{{total}}" size="large"  />

<van-cell-group title="学生列表">
  <van-cell  wx:for="{{ AllStu }}" wx:key="index" title="{{ item.name }}" is-link value="{{ item.grade }}" data-id="{{item.id}}" bind:click="goClassDetail"/>
</van-cell-group>
